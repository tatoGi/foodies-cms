# Laravel Database Layout

Expected minimal Laravel layout:

- `database/`
- `database/factories/`
- `database/migrations/`
- `database/seeders/`

Optional sqlite file (when `.env` uses sqlite):

- `database/database.sqlite`

This skill restores only missing directories and placeholders. It does not generate migration or seeder code.
